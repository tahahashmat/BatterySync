import React from 'react'
import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({})  //take this

    

const Drawer = () => {

    const classes = useStyles(); // and this

    return (
        <h1>Waleed</h1>
    )
}

export default Drawer